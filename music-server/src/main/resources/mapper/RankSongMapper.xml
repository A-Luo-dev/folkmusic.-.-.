<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yin.mapper.RankSongMapper">

    <!-- 字段映射 -->
    <resultMap id="BaseResultMap" type="com.example.yin.model.domain.RankSong">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="cover_img_url" property="coverImgUrl" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 公共字段列表 -->
    <sql id="Base_Column_List">
        id, name, cover_img_url, type, create_time
    </sql>

    <!-- 查询所有排行榜 -->
    <select id="getAllRankSong" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM rank_song
    </select>

    <!-- 根据ID查询排行榜 -->
    <select id="getRankSongById" parameterType="long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM rank_song
        WHERE id = #{id}
    </select>

</mapper>
